This programs asks for a input file name and opens it, reads the values inside it to the program. then calculates where spheres and lights are in the world and outputs a output.ppm image that shows how the spheres that are in the view behave with eachother and the light, for example if the light illuminates a sphere or if a sphere is in shadow of another sphere.

